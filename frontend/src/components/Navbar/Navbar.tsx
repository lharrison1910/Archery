import { AppBar, Box, Typography } from "@mui/material";

function Navbar() {
  return (
    <Box>
      <AppBar component="nav">
        <Typography>RAF Association Archery</Typography>
      </AppBar>
    </Box>
  );
}

export default Navbar;
